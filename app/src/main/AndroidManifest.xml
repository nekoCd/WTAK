<manifest xmlns:android="http://schemas.android.com/apk/res/android" package="com.WTAK.WebTerm">

    <application
        android:label="WTAK"
        android:icon="@mipmap/ic_launcher"
        android:theme="@style/Theme.WTAK">

        <activity
            android:name="com.WTAK.WebTerm.MainActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>
</manifest>
